const EmptyFieldMsg = () => {
  return (
    <span className="absolute right-0 left-0 text-right text-xs top-[1.3px] text-red-aura font-semibold">
      can't be empty
    </span>
  );
};

export default EmptyFieldMsg;
